Simply using `make` will make all three programs.
Or do `g++ lcs2.cpp timeProg.h -o program2`
Use by
./program2 <input-file1> <input-file2> <output-file>

Program2 doesn't use special data structures.
It uses recursion in a top-down approach to calculate the LCS for two strings.