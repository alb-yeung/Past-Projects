make will compile this program

It can be run with ./Kruskal <input-file>
SmallInput.txt, MedInput.txt, and Other.txt are provided

We used an adjacency matrix to store and keep track of the graph.
We used disjoint sets for cycle detection.